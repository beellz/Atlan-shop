apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: ecommerce
data:
  NODE_ENV: "production"
  MONGODB_HOST: "mongodb-service"
  MONGODB_PORT: "27017"
  RABBITMQ_HOST: "rabbitmq-service"
  RABBITMQ_PORT: "5672"
  API_VERSION: "v1"
  LOG_LEVEL: "info" 